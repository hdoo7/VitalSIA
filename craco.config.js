// craco.config.js - For illustration, likely unnecessary for face-api.js
module.exports = {
    webpack: {
        configure: (webpackConfig) => {
            // Example customization (usually not needed)
            return webpackConfig;
        },
    },
};
